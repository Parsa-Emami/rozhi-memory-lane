name: Deploy Rozhi Demo to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.3"
          extensions: mbstring, dom, fileinfo, sqlite, pdo_sqlite
          coverage: none
          tools: composer

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: "npm"

      - name: Prepare Laravel environment
        run: |
          cp .env.example .env
          echo "APP_ENV=production" >> .env
          echo "APP_DEBUG=false" >> .env
          echo "APP_URL=https://parsa-emami.github.io/rozhi-memory-lane" >> .env
          echo "ASSET_URL=https://parsa-emami.github.io/rozhi-memory-lane" >> .env
          echo "ROZHI_CAT_IMAGE=images/rozhin.jpg" >> .env

      - name: Install PHP dependencies
        run: composer install --no-dev --prefer-dist --optimize-autoloader --no-interaction

      - name: Generate Laravel key
        run: php artisan key:generate --force

      - name: Install Node dependencies
        run: npm ci --include=dev

      - name: Build Vite assets
        run: npm run build

      - name: Prepare static output folder
        run: |
          rm -rf _site
          mkdir -p _site
          cp -R public/* _site/
          touch _site/.nojekyll

      - name: Start Laravel local server
        run: |
          php artisan serve --host=127.0.0.1 --port=8000 > laravel.log 2>&1 &
          sleep 8
          curl -I http://127.0.0.1:8000

      - name: Export Laravel pages as static HTML
        run: |
          php -r '
          $base = "http://127.0.0.1:8000";
          $pages = [
              "/" => "index.html",
              "/story" => "story/index.html",
              "/memories" => "memories/index.html",
              "/quiz" => "quiz/index.html",
              "/letter" => "letter/index.html",
              "/future" => "future/index.html",
              "/invite" => "invite/index.html"
          ];

          foreach ($pages as $url => $file) {
              $html = @file_get_contents($base . $url);

              if ($html === false) {
                  fwrite(STDERR, "Failed to export: {$url}\n");
                  if (file_exists("laravel.log")) {
                      fwrite(STDERR, file_get_contents("laravel.log"));
                  }
                  exit(1);
              }

              $html = str_replace("http://127.0.0.1:8000", "https://parsa-emami.github.io/rozhi-memory-lane", $html);
              $html = str_replace("http://localhost", "https://parsa-emami.github.io/rozhi-memory-lane", $html);

              $path = "_site/" . $file;
              $dir = dirname($path);

              if (!is_dir($dir)) {
                  mkdir($dir, 0777, true);
              }

              file_put_contents($path, $html);
              echo "Exported {$url} -> {$path}\n";
          }
          '

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v6

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

  deploy:
    runs-on: ubuntu-latest
    needs: build

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4