Music file used by the app:

public/audio/lana-let-the-light-in.mp3

The app has only one audio element now. Playback starts from the bottom music dock button and is synchronized across browser tabs so the same song does not play two or three times at once.
